[user]
    email = alcarneyorg@gmail.com
    name = Alex Carney

[alias]
    co   = checkout
    hist = log --branches --remotes --tags --graph --oneline --decorate

[core]
    editor = nvim
    excludesfile = ~/.gitignore_global

[web]
    browser = chromium

[merge]
    tool = vimdiff
    conflictstyle = diff3
    prompt = false
